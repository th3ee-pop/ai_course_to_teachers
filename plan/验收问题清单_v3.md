# V3 制作验收问题清单（2026-09-15 夜）

来源：8 个制作代理的回报（`build/v3_batches/reports/A–H.md`）＋ 合并后的全页联络表复核（`build/qa/sheets/all_*.png`）。
分四类：**已修**（本轮已改公共文件）、**待你定夺**（脚本层或需作者素材）、**脚本文字问题**（只报告，JSON v0.3 未改）、**版面遗留**。

## 一、公共文件本轮已修

| 项 | 做法 | 影响页 |
|---|---|---|
| `build/template_tail.html` 未加载 `components/map334.js` | 已加一行 `<script src="components/map334.js">`；P024/P036 页内动态加载器已删；P078/P079 的静态 `.flow` 兜底被组件自动接管 | P024 P036 P078 P079（P095 仍为页内内联实现，未改回组件，原因见二.3） |
| 深色页页眉模块名不可见（`.locator .on` 与背景同色） | deck.css 加 `.dark .locator .on { color: var(--light-blue) }`；P024 页内临时规则已删 | 所有 `.slide.dark`：P000A P001 P024 P036 P068 P078 P079 P081 P095 等 |
| 演示截图接入 | 9 张截图已接入 P037A P042B P042D P042F P042H P090 P005A（`prod_kejian_1`）P051（`cap_guanchatai_talk`）；P066A 由批 F 改用动画代替图位，`cap_guanchatai_trace.png` 已截但未用 | — |
| P079 合并后版面溢出 | 紧凑版 map334 组件自带 `min-height:300px`，加上 268px 的材料图超出正文高度，`.body` 居中后上下同时盖住副标题和脚注。页内加了 P079 限定的覆盖：组件 `flex:0 0 auto; min-height:0`，材料图降到 220px，文字 24px。已重截核对 | P079 |
| P068 页内按需加载脚本 | 模板已加载 map334.js 后，删掉了 P068 的页内加载器和对应 PLAN 注释 | P068 |
| validate 误报 | `build/build.py` 动画检查改为：页内有 `class="map334"` 即视为已实现，P068 P078 P079 的 3 条固定 WARN 消失；现在 validate 0 error 0 warning | — |

## 二、待你定夺（脚本层 / 素材 / 公共规则）

1. **讲者备注回灌 v0.3**：多页 `aside.notes` 仍是 v0.2 讲稿，含旧术语（"管道／工具／主体""三种角色"）或与上屏内容对不上。代理按红线一字未改。建议由脚本侧统一把 v0.3 `speaker_notes` 回灌，一次做完。已点名的页：P005 P015（B）；P020 备注仍用"主体"（D）；P026 P036 P039（E）；P070 P078 P079 P087 P089（G）；P095 P096（H）。
2. **P023 备注指空**：批次备注要求 P023 做纯案例卡、不放 Word 缩略图，但 JSON `presentation` 和 `speaker_notes` 仍写"右边是这次委托实际得到的两份 Word 成品"。二选一：改备注为"成品放在 P026/P027 一起看"，或恢复缩略图。
3. **P095 总图仍是页内内联**：map334 组件渲染的环节名是长写法（教学设计／课堂实施／教学评价），v0.3 JSON 的 P095 是"设计、实施、评价"，逐字校验过不了，所以 P095 保留内联实现。若要统一为组件驱动，需先统一脚本用词。
4. **T17 页缺"典型需求"块的初态**：B10 B20 P089 的脚本没有典型需求，初态只剩标题副标题。要么脚本补一句典型需求，要么接受。
5. **P009 图标**："3D 打印机"用的是 iconlib 的等距模型图标（`printer3d.png`），不是打印机造型；若有更贴切的图可换。
6. **P020 达尔文对话截图**：缩到 244px 高后文字不可读，只当"有实拍"佐证；建议提供一张两三轮、字号更大的对话截图。
7. **P079 三处文档预览**（方案包／量规／周报）仍是 `slot[data-kind=doc]` 占位，需 Word 首页截图。
8. **P087/P086**：需一张真实纸质实验记录照片，现场用；错题扫描仪兜底录屏。
9. **P026 对照小图用词**（"笼统的委托／具体到本班"）是代理自拟，请确认。
10. **P037A 首图**：已换为 `cap_siji_curve_23.png`（单幅参与者界面）；原双联图 `siji_v2_xiazhi_dongzhi.png` 未删。
11. **`components/map433.js`** 仍含旧术语 ROLES（管道／工具／主体）与旧三问文案，已无页面使用，建议退役（本轮未删，怕仍有引用）。
12. ~~`plan/pages_plan.json` 的 map334 WARN~~ 已通过改 build.py 解决（见一），计划文件未动。
13. **deck.css `.corner-tag` 依赖祖先 `position:relative`**：P005 等页靠页内样式补；若后续常用，可在 deck.css 约定 `.tag-host`。
14. **`components/cycle.js`**：P015 的"第 n 轮"中心文字靠改 `data-center` 重绘；若再有计数环页面，建议加 `setCenter()` 接口。
15. **P002 政策截图张冠李戴**（批 A）：JSON 把 `cap_fjjyt_zhengce.png` 配给"政策三"，但截图内容实为政策二（教办〔2025〕3号，2025-07-09）。页面已按实际内容挂到政策二，政策一/三留占位，三份文号都写【待补】。另 `B04_zhengce_word_preview.png` 里日期是 2025-04-11，与网页 2025-07-09 不一致，请核对哪个是正式发文日期。
16. **ANIM_A 常显一行"流畅≠正确"未做**（批 A）：脚本 JSON 里没有这句，逐字校验不允许自拟，动画只做了其余部分。要么补进 JSON，要么接受。
17. **P011C 脚注含 `【待用户把关】`上屏**（批 C）：这是 JSON 原文，投影会直接看到；请给定稿文字。同页需要《望庐山瀑布》分镜文字（现为占位）。
18. **P014A 用了旧术语"工具"**（批 C）：正文"工具负责执行"与禁用旧词撞车，但它是 v0.3 JSON 原文，代理未改。请决定是否改脚本。
19. **iconlib 风格不合**（批 A）：iconlib 等距图标与封面/开场页风格冲突，批 A 改用手绘内联 SVG，没有再复制新图标；建议 deck.css 增加 `.cover-step`，把封面步进样式公共化。
20. **`.body.cols` 居中溢出**（批 C）：deck.css `.body.cols{align-items:center}` 让高内容页上下溢出，多页各自加 `align-items:stretch`；建议公共化为 `.body.cols.stretch`。
21. **批 F 公共建议**：`.flash-fig > .blueprint > img` 加 `object-fit:contain`（长图现靠 `.slot` 包一层绕开）；`.flow .node.on` 把底色与字色绑死，自定义底色的节点会白底白字，建议拆开。
22. **P045 无 link-card**（批 F）：《望庐山瀑布》视频只有本机 mp4，没有合法 config.js 键，代理刻意没放外链；现场要播需自备入口。
23. **P066 vs P066A**：两页同题同轨迹同结论，P066 不在装配序列里；请确认 P066 作废。
24. **`prod_kejian_2.png` 已截未接**：WorkBuddy 生成的《牛顿第一定律教学设计》docx 预览（14 页）。它是教学设计文档，不是课件页；P005A 只有一个产品图位（现放对话界面）。要用它需决定换图还是加图位。

## 三、脚本文字问题（只报告，JSON 未改）

### 批 B（P004 P005 P005A P009 P015）
- P004 脚注"标准操作流程，简称SOP。"孤立，正文没有出现 SOP；建议正文点出或脚注改为"以上四步是教师做资源的标准操作流程（SOP）"。
- P005 副标题"动画、图形、资源植入"三项不并列，"资源植入"偏行话。
- P005A 副标题引号样式与同模块其他页不一致，建议全课统一。
- P009 标题带句号而 P004/P015 不带；标题说三步（制作、试用、修改），图是四步（提出意图／形成原型／移植与修改／运行验证），标题与图不对齐。
- P009 副标题"3D打印机与放大器"两个比喻并列略挤，"放大器"在图上没有对应。
- P015 "所以"句"以后每次用AI，先问的不是…而是…"口语化、缺主语；正文"提供想法／下达任务／审核修订"与动画环"执行／核对／修订"两套词只有"修订"重合，需备注补一句衔接。

### 批 D（P017 P020 P021 P022 P023 P024）
- **P021 的 speaker_notes 与 P017 完全一样**（讲三类能力），与四个环节不符，应是复制遗留。
- P023 脚注"委托表达为教学示例"像半句话，建议"此处的委托表达为教学示例"。
- P024 PLAN 注释 reveal 列表是旧四步，与 v0.3 四块不一致（页面按 JSON 做，注释待同步）。
- P020 PLAN 注释与备注仍用"主体"。

### 批 E（P026 P036 P037A P039 P041 P042D/F/H）
- P037A 第三块"让抽象概念有了身体经验"主语错位；前三块不同类（两困难一解法）；脚注"截图为参与者界面"现已成立（截图已接入）。
- P036 三个 label 体例不一（名词短语 vs 主谓句）；副标题与第一块重复。
- P039 ③ "视角"歧义（实指三维模型观察视角）。
- P026 第二块"学生…；他们…"指代跳变，"怎样"可省。

### 批 G（P070 P076 P078 P079 P086 P087 P089）
- P076 脚注两句主语跳变（方法论＋图注混一行）。
- P089 "所以用到"把三类能力与三种动作一一配对，口述易被听成固定映射。
- P087 ① 与标题缺衔接，"谁来核对"主语从教师跳到学生。
- P079 副标题"四个环节，四件材料"，第四块 label"教师专业发展：周报"比前三块长，并排不齐；建议"发展：周报"。
- P070 ③ 一句里混两种情境（演示条件／真实课堂），分号前后易含混。

### 批 H（P095 P095A P096 B10 B20）
- B10 B20 缺典型需求块（见二.4）。
- B20 "所以用到"写法与 B10 不一致（label 不带能力名，能力名在 text 里）。
- B20 ③ 五个并列项过长，"读者"指向不明。
- P096 副标题一句三个"AI"拗口；脚注"三问"简写首次出现。
- P095 label 用"3类能力／3问／4环节"阿拉伯数字，与全场"三类能力／三问／四个环节"不一致。

### 批 A（P001 P000A P000B P002 P003 P003A P003B P003D P003F）
- P002 政策三配图实为政策二（见二.15）；三份文号 JSON 里都是空的。
- P003B "文章/文档"两处用词不一致，指同一份材料。
- P003F 一句里"但…但是"重复。
- P003D "它从哪里来"一句主语不清、读起来别扭。
- ANIM_A 常显行"流畅≠正确"不在 JSON（见二.16）。
- ~~P000A 主办单位·日期·地点、P000B 照片与三行简介均缺（见资源清单一）。~~ 2026-09-16 已按作者答复填入并接入，validate 0 错。

### 批 C（P011A P011B P011C P014A P014B）
- P011C 脚注原文带 `【待用户把关】`（见二.17）；望庐山分镜文字缺。
- P011B 副标题与第一块内容重复；树状图标"18 项 · 节选"为代理注明的节选口径，请确认。
- P014A "工具负责执行"用旧术语（见二.18）。
- P014B "它"指代跳变；收尾列出"虚拟教研员"但正文没有例子。
- AIGC 图已加角标：P011A `siji_v2_xiazhi_dongzhi.png`，P011C `pov_01_wanglushan.png`。

### 批 F（P044 P045 P051 P058 P064 P066A P068）
- P058 能力条"为什么选 time"：`time` 是 D08 初三第 24 题的选项，页面没有交代，单看不懂；建议脚本补题干指代。
- P058 正文"16,434 条作答记录"与脚注"脱敏聚合结果"并置，精确条数与脱敏口径是否一致请核对。
- P051 三问③后半句缺主语（应是 AI），单独上屏易读成对学生的要求。
- P066A 与 P066 重复（见二.23）。
- P045 P051 P058 P064 P068 备注仍是 v0.2 泛稿，P045 备注含旧词"工具"（并入二.1）。

## 四、版面遗留（合并后联络表复核）

- P089：三问卡下方约 300px 空白（T17 禁页内样式，暂接受）。
- P020：达尔文截图不可读（见二.6）。
- B10/B20/P089 初态只有标题（见二.4）。
- 全页联络表 `build/qa/sheets/all_00..11.png`（103 页）逐页看过：唯一真正的版面破损是 P079（已修，见一）。
- 仍是占位、等素材的格子：~~P000B 照片~~（已接入）；P002 政策一/三截图；P027 两张文件截图；P030 产品界面；P079 三份文档预览；P084 三张实拍。素材到位后即可接入，不需改版面。
- P053 内容偏少、留白多，属脚本层三问较短，暂接受。
- 其余页字号、留白、页眉页脚均正常，没有再发现溢出或重叠。
