批 H 完成。

## 1. 分支与提交
- 分支：`pages-v3-h`（worktree `.claude/worktrees/agent-a0690cb3ee77af354`），未 push、未合并。
- 提交：`a96fbb3` —「V3 批H：P095 P095A P096 B10 B20（附 P090 B13 备注演示前提）」，只提交了 `pages/` 与 `assets/img/icons/hand-push.png`（`index.html`、`build/manifest.json` 为 assemble 产物，按红线未提交）。

## 2. 每页情况
- **P095（T16 暗底，动画 ANIM_I）**：页头 PLAN 的「map433 / 三角色」已改为「map334 / 三类能力」。版面用 map334 的视觉语言**内联实现**：4环节横向流程（设计→实施→评价→教师专业发展，带真 `<marker>` 回环箭头）→ 3类能力一行三格 → 3问三列 → 进入下一轮一行。四步 `reveal data-step`，`→` 推进、「播放」自动、「重置」回初态，IIFE 挂 `window.__anim_P095`，MutationObserver 在离开本页时停表；三问与三类能力分区排列、无连线无同色。三问编号 01/02/03 用 CSS `::before` 输出，避免数字插进正文破坏 validate 的连续子串。素材：无图标，纯 CSS/SVG。占位：无。validate 0 ERROR；截图 `build/qa/P095.png`（?all）、`P095_step0.png`（初态）、`P095_mid.png`（第 2 步中间态）。遗留：见第 3 条 map334.js 未加载。
- **P095A（T05，skeleton→draft）**：内联多米诺 SVG 占正文约 55%：教师的手（图标 `assets/img/icons/hand-push.png`，取自 `assets/iconlib/02_教育学习/hand-up.png`，旋转 90° 作推的手势，`saturate(.75)`）＋真 `<marker>` 推力箭头＋「第一推」小标 → 已推倒的「真实问题」实底牌（带虚线运动弧）→ 依次倾斜的「三问 / 三类能力 / 四个环节」→ 尚未倒下的「学生的学习」。三个文字块改为三栏 `reveal`（图内文字 30px、说明 26px，均 ≥24px）。占位：无。validate 0 ERROR；截图 `build/qa/P095A.png`、`P095A_step0.png`。
- **P096（T01 暗底→T12 浅底，L08）**：按 `pages_plan.json` 改为浅底 T12（`DARK_TEMPLATES` 只含 T01/T16）。左栏是占正文约 60% 的大对话输入框（白底圆角＋分隔线＋灰色提示＋钢青「发送」图形，`aria-hidden`，不可点、不调模型，文末有一根光标竖线），模板文字 36px 全文照抄；右栏「现在就做」＋ `qr big[data-config=resourceUrl]`（保留原占位文案，未写任何真实地址）。模块与 locator 由「11 收束：下周的一件小事」改为「11 收束：第一推」。占位：二维码为 config.js 占位。validate 0 ERROR；截图 `build/qa/P096.png`。
- **B10（T06→T17）**：三问卡 `data-step 1/2/3` ＋ 底栏「所以用到：角色扮演」`data-step 4`；脚本无「典型需求」块，故省去 `.cc-need`（`.casecard` 是竖向 flex，少一段不会破版）。「示例对话（虚构）」用全局 `.card accent` 放在案例卡下方，英文句子用斜体区分，`data-step 5`。为放下第三问，页内作用域微调了 `.cc-q` 内边距与正文字号（30→28px，未低于 24px）。副标题按 v0.3 补了句号。validate 0 ERROR；截图 `build/qa/B10.png`、`B10_step0.png`。
- **B20（T06→T17）**：三问卡，其中②按 v0.3 换成「文件打开就能用：安排能排进课表，记录表能直接打印，周报里的每个数字都能回到来源。」；「所以用到」一句同时提到三类能力，按 T17 变体写成**一条** `.cc-cap`，label 并排三个 `.tag.fill`（连通管道／内容生成／角色扮演），text 逐字照抄。原有两张 `file-card`（D04 docx、D12 md）保留在底部一行，未删。validate 0 ERROR；截图 `build/qa/B20.png`、`B20_step0.png`。
- **P090、B13**：只在 `aside.notes` 末尾追加「演示前提：」段（各 3 句），分别取自 `plan/演示前提清单.md` 第 2、8 节；原有 `../19_逐页内容脚本/...` 与 `/Users/chensirui/...` 链接原样保留。P090 段写了：开启本场演示、二维码须是局域网地址而非回环地址、凭证不投影、听众选「同意记录/仅体验」且先自备 2–3 个会话、状态在内存重启即清空、可切「查看预制样例」。B13 段写了：需网络＋摄像头＋可用模型地址（Teachable Machine 训练、页面只做推理）、开讲前本机跑通一遍、网络不稳就不讲改用截图口述。均未写任何地址、密钥或 IP。validate 0 ERROR。

## 3. 本地待办新增项
**需作者提供的截图与信息**
- P096：`config.js` 的 `resourceUrl`（培训资源入口）现场未填，页面显示占位；需要作者在演讲机上填写。

**作者 AIGC 内容**
- 本批 5 页均未使用仓库内 AIGC 图片，因此没有角标需求；也没有因缺图而新增的占位。

**需要改公共文件的需求**
1. **`components/map334.js` 从未被加载**：`build/template_tail.html` 与 assemble 后的 `index.html` 只加载 `config.js / qrcode.min.js / map433.js / cycle.js / deck.js`，全仓库没有 `.map334` 用例。批次备注建议用 `Map334.set()/render()` 驱动 P095，但改 `build/` 属红线，故 P095 按同一视觉语言页内内联实现。若希望全场总图统一由组件出，需要总控在 `build/template_tail.html` 里加一行 `<script src="components/map334.js"></script>`，届时 P095 可改回组件驱动（注意组件渲染的是「教学设计/课堂实施/教学评价」等长写法，与 v0.3 JSON 的「设计、实施、评价」逐字不符，需先统一，否则 validate 过不了）。
2. **P095、P096 的 `aside.notes` 仍是 v0.3 之前的旧稿**：P095 备注写「三种角色」，P096 备注写「管道、工具和主体三种角色」，都是红线上的旧术语，且 P096 备注描述的还是旧结尾（「让AI服务真实的学习」已移到 P095A 的脚注）。红线规定备注只能在末尾追加、且只限点名页，故未动；建议总控统一重写这两页备注。
3. **T17 用于「无典型需求」的页时的初态问题**：B10/B20 的脚本没有「典型需求」块，按 T17 规则 `.cc-need` 缺席，初态整页只剩标题＋副标题（见 `B20_step0.png`）。如果希望初态有题面，需要在脚本里为这两页补一句「典型需求」（属脚本层决定），或由 `deck.css` 侧给无 `.cc-need` 的案例卡另一种初态；我未自创样式。

## 4. 脚本文字本身的问题（只报告，未改）
1. **B10 缺「典型需求」块**：同模块其他 T17 页都有题面，B10 直接从①开始，讲者初态无话可指；建议补一句典型需求。B20 同样缺。
2. **B20「所以用到」块的写法与别页不一致**：其 label 是「所以用到」（无冒号、无能力名），text 里才用「连通管道：…；内容生成：…；角色扮演：…」串起三类能力；而 B10 的 label 是「所以用到：角色扮演」。同一模板两种写法，做页时只能靠变体处理，建议统一。
3. **B20 的③「提供已有材料、读者、格式、期限、学校要求和应保留的来源。」**：连续五个并列项加一个「和」，读起来偏长，且「读者」在办公语境里指向不明（是审阅人还是分发对象）。
4. **P096 副标题「没关系。这正是AI的意义：AI会告诉你怎么用AI。」**：一句里出现三个「AI」，投影上略拗口；另脚注「这段话本身就是三问」与块里的模板文字确实对应（问题／成功标准／情况），逻辑无误，只是「三问」一词首次以简写出现在脚注里，若听众漏掉 P095 会有理解断层。
5. **P095 的 block label 用「3类能力／3问／4环节」阿拉伯数字简写，正文其余处一律用「三类能力／三问／四个环节」**，同页与全场存在数字写法不一致（仅记录，未改）。