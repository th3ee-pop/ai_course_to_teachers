批 C 五页全部完成并已在本分支提交（未 push、未合并）。

## 1. 分支与提交
- 分支：`pages-v3-c`（worktree：`.claude/worktrees/agent-ae215cc3a5c85dc5e`）
- 提交号：`cc40edce04baa40e43b3318d1d1a6bd7078aba5b`　标题：`V3 批C：P011A P011B P011C P014A P014B`
- 提交内容仅 `pages/*.html`（5 个）与 `assets/img/icons/*`（6 个新图标）；未动 deck.css / deck.js / components / build / 其他页面。

## 2. 每页做法

**P011A 代码也是符号，而且最规整**
左 546px 文字栏（为什么擅长 / 左边 / 这意味着）＋右侧代码窗。代码窗是从 `assets/tools/siji_v2/seasons-stage.js` 第 377–396 行原样截取的 20 行真实代码（`resize()`＋`geom()`＋`loop` 开头），等宽 22px 带真实行号，未作任何改写。左栏嵌 `assets/img/siji_v2_xiazhi_dongzhi.png`（作者 AIGC，加"AI生成试样"角标）。无占位。validate 0 ERROR；截图 `build/qa/P011A.png`、初态 `build/qa/P011A_init.png`。第二轮把左栏从 578px 收到 546px，解决第 382 行贴到面板右边框的问题。

**P011B Word、PPT、Excel，本质也是代码**
三栏：左文字（docx里有什么 / pptx与xlsx / 这意味着）＋三枚办公图标；中"解压目录"树；右"正文"XML 窗。目录树与 XML 均来自 `assets/docs/D04/天气观测活动安排（三年级）.docx` 的真实 zip 结构与 `word/document.xml` 开头四个 `<w:p>`，未改动，`document.xml` 用赭色高亮。图标：`document.png`／`slides.png`／`spreadsheet.png`。无占位。validate 0 ERROR；`build/qa/P011B.png`、`P011B_init.png`。第二轮修正：该 docx 实际 18 项，树里放得下 14 项，面板标注已由"18 items"改为"18 项 · 节选"以免误导；XML 窗底部加渐隐表示截断。

**P011C 图片和视频，也能由文字驱动**
左 55% 为内联 SVG 流程图（文字 → 表示空间 → 逐步还原 → 图像，真 `<marker>` 箭头；图像下方分支出"视频"四帧胶片），图内文字 26px；下方一行是"分镜文字 ↔ 成品图"对照：左侧虚线框保留 `【待用户提供：望庐山分镜文字】`，右侧用 `assets/pov_01_wanglushan.png` ＋"AI生成试样"角标。右 640px 五段文字。validate 0 ERROR；`build/qa/P011C.png`、`P011C_init.png`。

**P014A Agent：从"说话者"到"实干家"**
左侧内联 SVG（约 60% 正文宽）：大脑在上，真箭头分向手与脚，手→脚一条箭头，脚下一条回环并标注"运行、报错、再试"；三格分别用 `assets/img/icons/brain.png`、`hand.png`（来自 iconlib `hand-up.png`）、`runtime.png`（来自 iconlib `iteration.png`），格内标题 46px。右 640px 四段（大脑/手/脚/这意味着），分步 1–4。该页脚本无脚注，故无 `.foot`。无占位。validate 0 ERROR；`build/qa/P014A.png`、`P014A_init.png`。第二轮补了回环标注，原先回环无标签易误读。

**P014B 给它一个身份，它就按身份回应**
内联 HTML 对话框：顶行"同一个问题"气泡（40px）＋右上 `示例对话` 标签；中间两块身份面板并排，头像分别用 `assets/img/icons/assistant.png`、`p020_student.png`，回复用浅蓝气泡（35px）；底行"这意味着"。分步 1→4（问题→备课助手→虚拟学伴→这意味着）。对话内容逐字用脚本原文，未自造台词、未伪造模型记录，脚注保留"示例对话，不是真实模型运行记录。"。validate 0 ERROR；`build/qa/P014B.png`、`P014B_init.png`。第二轮整体放大字号填满版面。

五页 `data-status` 均已由 `skeleton` 改为 `draft`；全量 `validate` 中我的 5 页 0 ERROR（其余报错属别的批次页面）。

## 3. 本地待办新增项

**需作者提供的截图与信息**
- P011C：「望庐山」那一帧对应的**分镜文字/提示词原文**。页面已留 `【待用户提供：望庐山分镜文字】` 虚线占位；`plan/资源清单_v1.md` 第 60 行也把它列为未决问题。给了文字就能把"文字 ↔ 画面"的对照做实。
- P011C：脚注里原有的 `【待用户把关】` 原样保留，需作者确认"简化说明"的表述边界。

**作者 AIGC 内容（已用，需确认角标与出处口径）**
- P011A 用 `assets/img/siji_v2_xiazhi_dongzhi.png`（夏至/冬至两屏），角标"AI生成试样"。
- P011C 用 `assets/pov_01_wanglushan.png`，角标"AI生成试样"。
- P014B 的两段回复是脚本原文的示例对话，页面标了 `示例对话` 标签＋脚注声明，非真实运行记录，请确认这个标注口径。
- P011B 的 XML 与目录树、P011A 的代码片段都是仓库内真实文件的原样截取，未伪造，但 P011B 目录树为"18 项 · 节选"，如需完整 18 项需要另设版面（当前列高放不下 22 行且要跌破 24px 字号下限）。

**需要改公共文件的需求**
- 无。五页全部用 deck.css 现有类＋页内 `.slide[data-id="…"]` 作用域样式实现，未发现必须修改公共文件的需求。
- 仅记一条观察（不是需求）：`deck.css` 的 `.body.cols { align-items: center }` 会让整列内容比正文区高时上下双向溢出、盖住标题。凡满高分栏的页都必须在页内样式补 `align-items: stretch`。我的三页已各自补上；若后续批次频繁踩坑，可考虑在公共层加个 `.body.cols.stretch` 修饰类（本次未改）。

## 4. 脚本文字本身的问题（只报告，未改动）

1. **P014A「手」块用了"工具负责执行"**：`工具负责执行：读写文件、运行程序、生成文档与页面。` 这里的"工具"是 Agent 组件义，不是旧"三种角色"之一，但与红线里禁用的"工具"字面撞车，在同一课件里可能引起听众混淆。建议作者确认是否改成"外部工具"或"可调用的程序"之类的限定说法。
2. **P014B「设为虚拟学伴」块首字"它"指代偏移**：`它会用学生的口吻说：第二步我没听懂，为什么要先量一次？` 上一句"同一个问题"是老师问的，这里的"它"指模型，但紧接着模型说的是学生口吻的话，连读时"它=学生"与"它=模型"两层容易混。备注里同样是这个结构。
3. **P014B「这意味着」并列项与前文不齐**：正文只演示了"备课助手"和"虚拟学伴"两个身份，收束句却列了"备课助手、虚拟教研员、虚拟学伴"三个，"虚拟教研员"在画面上无对应示例（我按脚本原文照排，未增删）。
4. **P011C 脚注含【待用户把关】**：`简化说明，具体实现因模型而异。【待用户把关】` 该占位标记会直接出现在投影画面上，定稿前需要替换或删除。
5. **P011B 副标题与正文"XML"重复度高**：副标题"里面是一组XML文件"与「docx里有什么」的"解压之后是一组XML文件"几乎同句，投影时上下相邻略显重复。